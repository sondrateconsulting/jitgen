# Rust / Cargo
/target/
**/target/
*.rs.bk
# Bazel (symlinks only; MODULE.bazel.lock IS committed for reproducible canonical builds)
/bazel-*
# jitgen runtime state (never commit run state / overlays)
.jitgen/
# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
# Local env / secrets (never commit)
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa*
id_ed25519*
.netrc
.npmrc
.pypirc
.cargo/credentials*
credentials*.json
# Scratch
/tmp/
*.log
!docs/reviews/availability.log
# Python
__pycache__/
*.pyc
.venv/
venv/
